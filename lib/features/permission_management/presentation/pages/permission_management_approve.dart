import 'package:flutter/material.dart';

class PermissionManagementApprovePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text(
        'PermissionManagamentApprovePage',
        style: TextStyle(color: Colors.blue),
      )),
    );
  }
}
